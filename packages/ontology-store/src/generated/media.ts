// Generated by packages/ontology-store/scripts/generate-from-ttl.ts.
// Do not edit by hand.

import { Schema } from "effect";

export const ExcerptIri = Schema.String.pipe(
  Schema.check(Schema.isPattern(/^https:\/\/w3id\.org\/energy-intel\/excerpt\/[A-Za-z0-9_-]+$/)),
  Schema.brand("ExcerptIri")
);
export type ExcerptIri = typeof ExcerptIri.Type;

export const PodcastEpisodeIri = Schema.String.pipe(
  Schema.check(Schema.isPattern(/^https:\/\/w3id\.org\/energy-intel\/podcastEpisode\/[A-Za-z0-9_-]+$/)),
  Schema.brand("PodcastEpisodeIri")
);
export type PodcastEpisodeIri = typeof PodcastEpisodeIri.Type;

export const PodcastSegmentIri = Schema.String.pipe(
  Schema.check(Schema.isPattern(/^https:\/\/w3id\.org\/energy-intel\/podcastSegment\/[A-Za-z0-9_-]+$/)),
  Schema.brand("PodcastSegmentIri")
);
export type PodcastSegmentIri = typeof PodcastSegmentIri.Type;

export const ConversationIri = Schema.String.pipe(
  Schema.check(Schema.isPattern(/^https:\/\/w3id\.org\/energy-intel\/conversation\/[A-Za-z0-9_-]+$/)),
  Schema.brand("ConversationIri")
);
export type ConversationIri = typeof ConversationIri.Type;

export const MediaAttachmentIri = Schema.String.pipe(
  Schema.check(Schema.isPattern(/^https:\/\/w3id\.org\/energy-intel\/mediaAttachment\/[A-Za-z0-9_-]+$/)),
  Schema.brand("MediaAttachmentIri")
);
export type MediaAttachmentIri = typeof MediaAttachmentIri.Type;

export const PostIri = Schema.String.pipe(
  Schema.check(Schema.isPattern(/^https:\/\/w3id\.org\/energy-intel\/post\/[A-Za-z0-9_-]+$/)),
  Schema.brand("PostIri")
);
export type PostIri = typeof PostIri.Type;

export const ScreenshotIri = Schema.String.pipe(
  Schema.check(Schema.isPattern(/^https:\/\/w3id\.org\/energy-intel\/screenshot\/[A-Za-z0-9_-]+$/)),
  Schema.brand("ScreenshotIri")
);
export type ScreenshotIri = typeof ScreenshotIri.Type;

export const ChartIri = Schema.String.pipe(
  Schema.check(Schema.isPattern(/^https:\/\/w3id\.org\/energy-intel\/chart\/[A-Za-z0-9_-]+$/)),
  Schema.brand("ChartIri")
);
export type ChartIri = typeof ChartIri.Type;

export const GenericImageAttachmentIri = Schema.String.pipe(
  Schema.check(Schema.isPattern(/^https:\/\/w3id\.org\/energy-intel\/genericImageAttachment\/[A-Za-z0-9_-]+$/)),
  Schema.brand("GenericImageAttachmentIri")
);
export type GenericImageAttachmentIri = typeof GenericImageAttachmentIri.Type;

export const SocialThreadIri = Schema.String.pipe(
  Schema.check(Schema.isPattern(/^https:\/\/w3id\.org\/energy-intel\/socialThread\/[A-Za-z0-9_-]+$/)),
  Schema.brand("SocialThreadIri")
);
export type SocialThreadIri = typeof SocialThreadIri.Type;

export const EvidenceSourceIri = Schema.String.pipe(
  Schema.check(Schema.isPattern(/^https:\/\/w3id\.org\/energy-intel\/evidenceSource\/[A-Za-z0-9_-]+$/)),
  Schema.brand("EvidenceSourceIri")
);
export type EvidenceSourceIri = typeof EvidenceSourceIri.Type;

/**
 * excerpt
 *
 * A text excerpt (quote / paragraph / table cell) embedded in or attached to a Post. Re-parented to iao:TextualEntity.
 */
export class Excerpt extends Schema.Class<Excerpt>("Excerpt")({
  iri: ExcerptIri,
  excerptFrom: Schema.optional(Schema.String)
}) {}

/**
 * podcast episode
 *
 * A discrete podcast episode, modeled as both an iao:Document and a Conversation.
 */
export class PodcastEpisode extends Schema.Class<PodcastEpisode>("PodcastEpisode")({
  iri: PodcastEpisodeIri
}) {}

/**
 * podcast segment
 *
 * A bounded temporal span of a PodcastEpisode that can evidence a CMC and can be spoken by one or more Experts.
 */
export class PodcastSegment extends Schema.Class<PodcastSegment>("PodcastSegment")({
  iri: PodcastSegmentIri,
  hasSegmentIndex: Schema.optional(Schema.Number),
  spokenBy: Schema.optional(Schema.String),
  partOfEpisode: Schema.optional(PodcastEpisodeIri)
}) {}

/**
 * conversation
 *
 * Abstract parent of multi-turn communicative artefacts (social threads, podcast episodes).
 */
export class Conversation extends Schema.Class<Conversation>("Conversation")({
  iri: ConversationIri
}) {}

/**
 * media attachment
 *
 * Abstract parent of image/text artefacts attached to a Post and potentially evidencing a CMC.
 */
export class MediaAttachment extends Schema.Class<MediaAttachment>("MediaAttachment")({
  iri: MediaAttachmentIri
}) {}

/**
 * post
 *
 * A social-media or short-form document authored by an Expert that may evidence CMCs. Re-parented to iao:Document and ei:EvidenceSource.
 */
export class Post extends Schema.Class<Post>("Post")({
  iri: PostIri,
  inConversation: Schema.optional(Schema.Union([ConversationIri, SocialThreadIri, PodcastEpisodeIri])),
  presents: Schema.optional(Schema.Union([MediaAttachmentIri, ExcerptIri, ScreenshotIri, ChartIri, GenericImageAttachmentIri])),
  repliesTo: Schema.optional(PostIri),
  authoredBy: Schema.optional(Schema.String)
}) {}

/**
 * screenshot
 *
 * A raster capture of a displayed document, UI, or data table.
 */
export class Screenshot extends Schema.Class<Screenshot>("Screenshot")({
  iri: ScreenshotIri,
  screenshotOf: Schema.optional(Schema.String)
}) {}

/**
 * chart
 *
 * A data-visualisation image attached to a Post. Re-parented to iao:Image.
 */
export class Chart extends Schema.Class<Chart>("Chart")({
  iri: ChartIri
}) {}

/**
 * generic image attachment
 *
 * Catch-all image attachment not classifiable as Chart or Screenshot (renamed from ei:Image 2026-04-22).
 */
export class GenericImageAttachment extends Schema.Class<GenericImageAttachment>("GenericImageAttachment")({
  iri: GenericImageAttachmentIri
}) {}

/**
 * social thread
 *
 * A directed sequence of Posts forming a reply / quote / repost graph on a social platform.
 */
export class SocialThread extends Schema.Class<SocialThread>("SocialThread")({
  iri: SocialThreadIri
}) {}

/**
 * evidence source
 *
 * Abstract parent of any information content entity that can stand as the evidencing artefact for a CanonicalMeasurementClaim. Three asserted subclasses in V0: Post, MediaAttachment, PodcastSegment.
 */
export class EvidenceSource extends Schema.Class<EvidenceSource>("EvidenceSource")({
  iri: EvidenceSourceIri
}) {}
